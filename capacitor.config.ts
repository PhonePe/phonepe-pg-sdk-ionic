import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'phonepe payment capacitor example',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
